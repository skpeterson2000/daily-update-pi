# Disable wifi power-save on all wifi connections.
# Power-save was putting wlan0 to sleep when idle, causing the daily-update
# connectivity probe to time out on every unattended run (radio wake > 10s).
# NM values: 2 = disable, 3 = enable.
[connection]
wifi.powersave = 2
